# Веб-вьювер гистосканов

Прототип по техническому заданию [PRD.md](PRD.md): просмотр сканов Aperio SVS в браузере с подгрузкой только видимых фрагментов. Для обучения и разборов, не для первичной диагностики.

## Что сделано в первой итерации

| Раздел ТЗ | Состояние |
| --- | --- |
| Тайл-сервер (FastAPI + OpenSlide, DeepZoom) | готов, с дисковым кэшем тайлов и лимитом объёма |
| Адаптер хранилища | готов для папки на диске сервера (основной вариант по ТЗ) |
| Навигация, раздел 6 (Н-1…Н-10) | готово, включая поворот на 90° |
| Настройка изображения, раздел 7 (И-1…И-11) | готово, включая пипетку белого |
| Вход по паролю, роли, отказ без авторизации | готово |
| Простой список слайдов, синхронизация с хранилищем | готово |
| Журнал открытий слайдов (К-9) | пишется в базу, экрана просмотра пока нет |

Ещё не сделано: утилита деидентификации, развёртывание на VPS казахстанского провайдера (файлы Docker написаны, но не проверялись), каталог случаев с фильтрами и «слепым» режимом (этап 2), адаптер S3 (необязательный этап 3). Состояние работ и ближайшие шаги ведутся в [CLAUDE.md](CLAUDE.md).

## Запуск на Windows

Нужен Python 3.12. Из папки проекта:

```
python -m venv .venv
.venv\Scripts\python.exe -m pip install -r requirements.txt
copy config.example.yaml config.yaml
```

В `config.yaml` укажите папку со сканами (`storage.root`) и для запуска по http поставьте `auth.https_only: false`. Затем создайте пользователя и запустите сервер:

```
.venv\Scripts\python.exe -m server.manage add-user ivanov --role admin --name "Иванов И. И."
.venv\Scripts\python.exe -m uvicorn server.main:app --port 8000
```

Вьювер откроется по адресу http://localhost:8000.

Управление пользователями: `add-user`, `set-password`, `del-user`, `list-users`. Роли: `resident` (просмотр), `teacher` (просмотр и ссылки на поле зрения), `admin` (всё, включая кнопку «Обновить список»). Самостоятельной регистрации нет.

Проверка после изменений: `.venv\Scripts\python.exe -m pip install -r requirements-dev.txt`, затем `.venv\Scripts\python.exe -X utf8 tests\smoke_test.py`.

## Конфигурация

Все параметры с пояснениями перечислены в [config.example.yaml](config.example.yaml). Путь к файлу можно задать переменной `VIEWER_CONFIG`. Секреты в файл не пишутся: ключ подписи сессий берётся из `VIEWER_SECRET_KEY`, а если переменной нет, создаётся в `data/secret.key`.

В папке `data/` лежат база каталога `viewer.sqlite3`, кэш тайлов и миниатюр. Кэш можно удалять в любой момент, базу нужно резервировать.

## Имена файлов

Шаблон из ТЗ: `<ИД пациента>_<номер стекла>_<окраска>.svs`, например `P004512_S03_HE.svs`. Из такого имени каталог заполняет поля слайда (код случая, стекло, окраска), и файлы группируются по случаям.

Само имя файла по ТЗ нигде не отображается, не передаётся в браузер и не пишется в журнал сервера: в нём могут оказаться персональные данные. Файл с именем не по шаблону получает нейтральное название вида «Слайд 9FC486». Адреса страниц содержат только внутренний идентификатор слайда.

## Устройство

```
server/
  main.py       маршруты: страницы, вход, каталог, тайлы, миниатюры
  storage.py    адаптер хранилища («перечислить слайды», «открыть слайд по ключу»)
  slides.py     пул открытых слайдов, метаданные, синхронизация каталога
  tiles.py      нарезка на тайлы DeepZoom
  tilecache.py  дисковый кэш тайлов с вытеснением по давности
  auth.py       пароли (bcrypt), сессии, ограничение перебора
  db.py         SQLite: слайды, пользователи, журнал
  manage.py     управление учётными записями
web/
  pages/        login.html, index.html (каталог), viewer.html
  static/js/    viewer.js, adjust.js (WebGL-шейдер), adjust-panel.js, catalog.js, i18n.js
  static/vendor OpenSeadragon 5.0.1 (BSD)
```

Браузер обращается только к тайл-серверу, к хранилищу напрямую никогда. Поэтому перенос сканов с диска сервера в бакет затронет один модуль `storage.py`.

**Почему свой генератор тайлов.** У Aperio SVS шаг уровней пирамиды чуть больше целого (4,0001 вместо 4). Стандартный `DeepZoomGenerator` из-за этого читает полное разрешение и сжимает его: около 190 мс на тайл 10×. `tiles.py` выбирает уровень с допуском 2 % и читает тайл напрямую: 15–30 мс. Геометрия тайлов совпадает со стандартной.

**Увеличение.** `увеличение = объектив сканирования × (экранных пикселей на пиксель изображения)`, как в ImageScope: на 40× один пиксель скана равен одному пикселю экрана. Объектив берётся из метаданных, при его отсутствии вычисляется из размера пикселя (0,25 мкм ≈ 40×), иначе показывается процент от полного разрешения. Цифровой зум ограничен 2× от увеличения сканирования.

**Настройка изображения.** OpenSeadragon рисует тайлы на 2D-canvas; после каждого кадра он копируется в текстуру и перерисовывается фрагментным шейдером на WebGL-canvas поверх. Тайлы повторно не загружаются, мини-карта и миниатюры остаются без коррекций. Порядок применения и формулы:

1. Баланс каналов: `c × 2^(b/100)`, усиление от 0,5 до 2.
2. Яркость и контраст: `(c − 0,5) × 3^(контраст/100) + 0,5 + яркость/200`.
3. Гамма: `c^(1/гамма)`, значения больше 1 осветляют средние тона.
4. Насыщенность: смешение с яркостью (Rec. 709) с коэффициентом `1 + s/100`; затем оттенок: поворот вокруг серой оси.

Настройки хранятся в браузере отдельно для пользователя и слайда. Значения пресетов в `adjust.js` предварительные, их нужно согласовать на тестовых сканах.

**Горячие клавиши.** `0` Fit, `1`–`5` 2×/4×/10×/20×/40×, `+`/`−` зум, стрелки панорамирование, `F` полный экран, `R` сброс вида. Работают в любой раскладке.

**Интерфейс на других языках.** Все строки лежат в словаре `static/js/i18n.js`; для казахского или английского добавляется словарь с теми же ключами.

## Развёртывание на VPS (не проверялось)

Нужны VPS казахстанского провайдера с Docker и доменное имя, направленное на сервер.

1. Создать на сервере папку сканов `/mnt/slides` и учётную запись SFTP, ограниченную этой папкой. Сканы загружаются по SFTP, например через WinSCP.
2. Скопировать на сервер папку проекта без `.venv` и `data`. В `config.yaml`: `storage.root: /mnt/slides`, `auth.https_only: true`.
3. В `.env` рядом с `docker-compose.yml`: `VIEWER_DOMAIN=viewer.example.kz` и `VIEWER_SECRET_KEY=<случайная строка>`.
4. `docker compose up -d --build`, затем создать администратора:
   `docker compose exec viewer python -m server.manage add-user admin --role admin`

Caddy получает сертификат HTTPS автоматически. Перезапуск сервиса обеспечивает `restart: unless-stopped`.
