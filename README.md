# Веб-сервис хранения и просмотра гистосканов

Сервис по техническому заданию [PRD.md](PRD.md): администратор загружает сканы Aperio SVS, раскладывает их по папкам и выдаёт доступ; пользователи смотрят доступные им сканы в браузере с подгрузкой только видимых фрагментов. Для обучения и разборов, не для первичной диагностики.

## Состояние работ

| Раздел ТЗ | Состояние |
| --- | --- |
| Тайл-сервер (FastAPI + OpenSlide, DeepZoom) | готов, с дисковым кэшем тайлов и лимитом объёма |
| Навигация, раздел 6 (Н-1…Н-10) | готово, включая поворот на 90° |
| Настройка изображения, раздел 7 (И-1…И-11) | готово, включая пипетку белого |
| Папки и права доступа, раздел 10 (Д-1…Д-8) | серверная часть готова и покрыта тестами |
| Учётные записи и группы, раздел 10 (П-1…П-10, Д-9, Д-10) | серверная часть готова: роли, группы, выдача паролей, блокировка, срок действия |
| Журнал действий (Д-8) | пишется в базу, экран просмотра в работе |
| Загрузка сканов через браузер, раздел 9 | серверная часть готова: части по 8 МБ, докачка, проверка места |
| Интерфейс каталога и экраны администратора | в работе |
| Двухэкранный режим, раздел 8 | этап 3 |

Ближайшие шаги ведутся в [CLAUDE.md](CLAUDE.md).

## Запуск на Windows

Нужен Python 3.12. Из папки проекта:

```
python -m venv .venv
.venv\Scripts\python.exe -m pip install -r requirements.txt
copy config.example.yaml config.yaml
```

Для запуска по http поставьте в `config.yaml` `auth.https_only: false`. Затем создайте первого администратора и запустите сервер:

```
.venv\Scripts\python.exe -m server.manage add-user ivanov --role admin --name "Иванов И. И."
.venv\Scripts\python.exe -m uvicorn server.main:app --port 8000
```

Сервис откроется по адресу http://localhost:8000. Сканы загружает администратор через веб-интерфейс: папку хранилища заполнять вручную не нужно.

В командной строке создаётся только первый администратор, дальше учётные записи заводит администратор в интерфейсе. Команды: `add-user`, `set-password`, `del-user`, `list-users`. Роли: `user` (просмотр доступных сканов) и `admin` (всё). Самостоятельной регистрации нет.

Пользователи объединяются в группы: при создании базы заводятся «Патологи», «Гематологи», «Резиденты», администратор может создать свои. Доступ выдаётся пользователям и группам; действует объединение прав. «Администраторы» это не группа, а роль: у неё доступ есть всегда.

Проверка после изменений: `.venv\Scripts\python.exe -m pip install -r requirements-dev.txt`, затем

```
.venv\Scripts\python.exe -X utf8 tests\access_test.py
.venv\Scripts\python.exe -X utf8 tests\upload_test.py
.venv\Scripts\python.exe -X utf8 tests\smoke_test.py
```

Тесты прав и загрузки работают на временной базе и временном хранилище и собирают тестовый SVS сами, поэтому рабочие данные и ваши сканы не затрагиваются.

## Конфигурация

Все параметры с пояснениями перечислены в [config.example.yaml](config.example.yaml). Путь к файлу можно задать переменной `VIEWER_CONFIG`. Секреты в файл не пишутся: ключ подписи сессий берётся из `VIEWER_SECRET_KEY`, а если переменной нет, создаётся в `data/secret.key`.

В папке `data/` лежат база каталога `viewer.sqlite3`, кэш тайлов и миниатюр. Кэш можно удалять в любой момент, базу нужно резервировать.

## Папки, названия и доступ

Дерево папок хранится в базе, а файлы лежат на диске под внутренними идентификаторами (`ab/ab12cd34ef56.svs`). Поэтому переименование и перемещение папок мгновенны и не ломают ссылки на поле зрения.

Исходное имя файла может содержать фамилию, поэтому оно не попадает ни в пути, ни в адреса страниц, ни в ответы сервера обычному пользователю, ни в журнал. В карточке скана его видит только администратор. Название в каталоге задаёт администратор; по умолчанию подставляется нейтральное («Скан 01»). Если имя файла было по шаблону `<код>_<стекло>_<окраска>.svs`, поля заполняются из него.

Доступ задаётся папке или отдельному скану: «только администраторы», «все пользователи» или «выбранные». Подпапка без своего режима наследует режим папки выше, а заданный у неё режим полностью заменяет унаследованный: внутри папки «для всех» можно закрыть одну подпапку. Право проверяется на каждый запрос, включая тайлы и этикетку; чужой скан отвечает так же, как несуществующий.

## Устройство

```
server/
  main.py       маршруты: страницы, вход, каталог, тайлы, миниатюры, этикетка, админские операции
  access.py     права доступа: действующий режим, наследование, видимость папок
  catalog.py    дерево папок, карточки сканов, проверка целостности хранилища
  groups.py     группы пользователей
  uploads.py    приём сканов частями с докачкой
  storage.py    адаптер хранилища: сохранить, открыть, удалить, свободное место
  slides.py     пул открытых слайдов и чтение метаданных
  tiles.py      нарезка на тайлы DeepZoom
  tilecache.py  дисковый кэш тайлов с вытеснением по давности
  auth.py       пароли (bcrypt), сессии, состояние учётной записи, ограничение перебора
  audit.py      журнал действий
  db.py         SQLite: схема с версией, миграции
  manage.py     первый администратор из командной строки
web/
  pages/        login.html, index.html (каталог), viewer.html
  static/js/    viewer.js, adjust.js (WebGL-шейдер), adjust-panel.js, catalog.js, i18n.js
  static/vendor OpenSeadragon 5.0.1 (BSD)
```

Браузер обращается только к приложению, к хранилищу напрямую никогда. Поэтому перенос сканов в бакет затронет один модуль `storage.py`.

**Почему свой генератор тайлов.** У Aperio SVS шаг уровней пирамиды чуть больше целого (4,0001 вместо 4). Стандартный `DeepZoomGenerator` из-за этого читает полное разрешение и сжимает его: около 190 мс на тайл 10×. `tiles.py` выбирает уровень с допуском 2 % и читает тайл напрямую: 15–30 мс. Геометрия тайлов совпадает со стандартной.

**Увеличение.** `увеличение = объектив сканирования × (экранных пикселей на пиксель изображения)`, как в ImageScope: на 40× один пиксель скана равен одному пикселю экрана. Объектив берётся из метаданных, при его отсутствии вычисляется из размера пикселя (0,25 мкм ≈ 40×), иначе показывается процент от полного разрешения. Цифровой зум ограничен 2× от увеличения сканирования.

**Настройка изображения.** OpenSeadragon рисует тайлы на 2D-canvas; после каждого кадра он копируется в текстуру и перерисовывается фрагментным шейдером на WebGL-canvas поверх. Тайлы повторно не загружаются, мини-карта и миниатюры остаются без коррекций. Порядок применения и формулы:

1. Баланс каналов: `c × 2^(b/100)`, усиление от 0,5 до 2.
2. Яркость и контраст: `(c − 0,5) × 3^(контраст/100) + 0,5 + яркость/200`.
3. Гамма: `c^(1/гамма)`, значения больше 1 осветляют средние тона.
4. Насыщенность: смешение с яркостью (Rec. 709) с коэффициентом `1 + s/100`; затем оттенок: поворот вокруг серой оси.

Настройки хранятся в браузере отдельно для пользователя и слайда. Значения пресетов в `adjust.js` предварительные, их нужно согласовать на тестовых сканах.

**Горячие клавиши.** `0` Fit, `1`–`5` 2×/4×/10×/20×/40×, `+`/`−` зум, стрелки панорамирование, `F` полный экран, `R` сброс вида. Работают в любой раскладке.

**Интерфейс на других языках.** Все строки лежат в словаре `static/js/i18n.js`; для казахского или английского добавляется словарь с теми же ключами.

## Развёртывание на VPS (не проверялось)

Нужны VPS казахстанского провайдера с Docker и доменное имя, направленное на сервер.

1. Создать на сервере папку сканов `/mnt/slides` и учётную запись SFTP, ограниченную этой папкой. Сканы загружаются по SFTP, например через WinSCP.
2. Скопировать на сервер папку проекта без `.venv` и `data`. В `config.yaml`: `storage.root: /mnt/slides`, `auth.https_only: true`.
3. В `.env` рядом с `docker-compose.yml`: `VIEWER_DOMAIN=viewer.example.kz` и `VIEWER_SECRET_KEY=<случайная строка>`.
4. `docker compose up -d --build`, затем создать администратора:
   `docker compose exec viewer python -m server.manage add-user admin --role admin`

Caddy получает сертификат HTTPS автоматически. Перезапуск сервиса обеспечивает `restart: unless-stopped`.
