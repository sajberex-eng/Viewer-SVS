# Веб-вьювер гистосканов: контекст для Claude Code

Этот файл переносится вместе с папкой проекта и заменяет историю переписки. Перед работой прочитай его, затем [PRD.md](PRD.md) (техническое задание, редакция 4; план работ — раздел 13) и [README.md](README.md) (устройство, запуск, развёртывание; соответствует коду).

С заказчиком общаемся по-русски. Заказчик не разработчик: команды и шаги давай готовыми, без предположения, что установлены git, Docker или Node.

## Что это

Веб-сервис хранения и просмотра сканов Aperio SVS: администратор загружает сканы через браузер, раскладывает по папкам, выдаёт логины и доступ; пользователи только смотрят. Тайл-сервер на FastAPI и OpenSlide, клиент на чистом HTML/JS с OpenSeadragon, без сборки.

Репозиторий: `github.com/sajberex-eng/Viewer-SVS`, публичный, ветка `main`. В него не должны попадать сканы, `data/`, `config.yaml`, `.env` (исключены в `.gitignore`). Push только вторым токеном по правилам из глобального `CLAUDE.md` пользователя; `github.com:443` с этого компьютера отвечает нестабильно, push при тайм-ауте повторять.

## Решения заказчика

Редакция 3 (2026-09-19):

- Хостинг: is*hosting (ishosting.com), VPS в Казахстане (Алматы) куплен 2026-09-19: тариф Start, Xeon 2 × 2,60 ГГц, 2 ГБ ОЗУ, диск 30 ГБ SSD (не 40, как заказчик говорил раньше), 1 Гбит/с, 3 ТБ трафика, Ubuntu 24, KVM. Необслуживаемый: без панели и администрирования, без защиты от DDoS, копии провайдера еженедельные, SSH пока по паролю root, VNC не подключён. Под сканы остаётся около 16 ГБ: хранилище это рабочая полка, а не архив. Бюджет диска, меры под 2 ГБ памяти и таблица параметров сервера в разделе 4 ТЗ. Адрес сервера и учётные данные в репозиторий не записывать: он публичный.
- Сканы загружает администратор через веб-интерфейс, частями, с докачкой. Принимаемый файл пишется сразу на место и переименовывается, без второй копии.
- Папки и подпапки создаёт администратор; в названиях нет ФИО. Дерево папок живёт в базе, файлы на диске под внутренними ID.
- Две роли: администратор и пользователь (только просмотр). Логины и пароли выдаёт администратор в веб-интерфейсе.
- Доступ к папке или скану: только администраторы, все пользователи, выбранные пользователи; наследуется от папки, свой режим заменяет унаследованный. Проверка на сервере для сведений, тайлов и миниатюр.
- Файлы загружаются как есть, с этикетками. Заказчик решил: сервер физически в Казахстане, поэтому данные казахстанских пациентов на нём хранить можно. Этикетка (label) показывается во вьювере по кнопке тем, у кого есть доступ к скану; открытие пишется в журнал; отдаётся с `Cache-Control: no-store`. Macro-изображение не отдаётся никому. Не выяснено, где провайдер хранит еженедельные копии VPS.
- Двухэкранный режим (раздел 8 ТЗ): два скана рядом, независимая или связанная навигация с пересчётом в микрометрах, настройки изображения по половинам. Только клиентская часть: два экземпляра OpenSeadragon и два слоя `adjust.js`.
- Домен пока не регистрируется: работаем по IP-адресу сервера, ссылку заказчик рассылает сам. Целевое имя `hemcenterhistodigital.kz`. Сертификат на IP: сначала пробуем Let's Encrypt с профилем `shortlived`, запасной вариант — самоподписанный сертификат Caddy с предупреждением браузера; по HTTP не работаем никогда. Порядок работ: этап 1 сервер, этап 2 хранилище и доступ, этап 3 двухэкранный режим. 16 ГБ под сканы заказчик считает достаточными для тестирования.
- Парольный вход по SSH оставлен включённым и VNC не подключается — решение заказчика, риск принят (за первый день 89 попыток подбора, смягчает fail2ban). Не отключать без его прямого указания.
- Название в интерфейсе: HemCenterHistoDigital. Копии VPS у провайдера хранятся в Казахстане (со слов заказчика). Вход в сервис по логину и паролю, без ограничения по IP. Вход на сервер по SSH-ключам.
- Форматы (2026-09-19): кроме SVS принимаются однофайловые форматы, которые читает OpenSlide (`.ndpi`, `.scn`, `.tiff`, `.tif`, `.bif`, `.czi`, `.avs`, `.svslide`; список `SLIDE_EXTENSIONS` в `storage.py` и `upload.js`). Многофайловые (MRXS, VMS/VMU, DICOM, Trestle) — нет. Файл хранится со своим расширением: NDPI и KFB узнаются по нему.
- KFB (2026-09-19, решение заказчика; риски приняты и записаны в разделе 3 ТЗ): читается через KFB-часть ASlide (GPL-3.0) с закрытой библиотекой KFBio, только Linux. `server/kfb.py` — обёртка под интерфейс OpenSlide, чтение под замком. В образ ставит `tools/install_aslide_kfb.py`: коммит и SHA-256 файлов закреплены, файлы качаются с `raw.githubusercontent.com` (лимита API нет), создаётся ссылка `libcurl.so.4`. Этикетку читать только своим кодом (`_AssociatedImages`): ASlide освобождает чужой буфер, и второе чтение роняет процесс. На Windows KFB недоступен — проверять в образе: `docker build`, файл монтировать `:ro`. Проверено на настоящем скане заказчика (в отчётах — только по ID, файл не копировать).
- Деидентификация и утилита для неё исключены. Каталог случаев с диагнозом и «слепым» режимом исключён.

Действуют по-прежнему:

- Исходные имена файлов не отображаются пользователям, не попадают в пути, адреса, ответы сервера пользователям и журнал сервера. В редакции 3 исходное имя хранится в базе и видно только администратору.
- Клиент без сборки и без Node. Локальная разработка на Windows в `.venv`, без Docker. Файлы Docker пишутся для VPS.
- Тестовые сканы заказчика содержат фамилии в именах части файлов и label-изображения. Их можно только читать локально. Не копировать, не загружать куда-либо (тем более в публичный репозиторий), не называть по имени файла в отчётах: только по ID слайда.

## Этап 2: что уже сделано в коде (2026-09-19)

Серверная часть фундамента готова и покрыта тестами; интерфейс к ней приведён (список ниже).

- `db.py`: схема версионируется через `PRAGMA user_version`, версия 1. Таблицы `folders`, `access_grants`, `audit_log`, расширенные `users` и `slides`. Миграция с базы редакции 2 автоматическая.
- `access.py`: `AccessIndex` — режим доступа папки или скана, наследование, видимость папок. Свой режим заменяет унаследованный; разрешения действуют там же, где задан режим.
- `catalog.py`: дерево папок, карточки сканов, права, проверка целостности хранилища.
- `storage.py`: адаптер умеет сохранять, удалять и считать свободное место; файлы под внутренними ID (`ab/ab12cd34ef56.svs`), имя файла в путь не попадает.
- `auth.py`: роли `user` и `admin`, состояние записи, срок действия, обязательная смена выданного пароля, `session_epoch` (блокировка и смена пароля рвут сессии сразу).
- `audit.py`: журнал действий, переживает удаление пользователя.
- `main.py`: право проверяется на каждый запрос — сведения, тайл, миниатюра, этикетка. Чужой скан отвечает 404, как несуществующий.
- `groups.py`: группы пользователей. При создании базы заводятся «Патологи», «Гематологи», «Резиденты»; администратор создаёт свои. «Администраторы» в таблице нет: это роль `admin`, доступ у неё есть всегда, название зарезервировано. Право пользователя — объединение его прямых разрешений и разрешений всех его групп.
- `uploads.py`: приём файла частями по 8 МБ в один временный файл, докачка с последней принятой части, контрольная сумма каждой части, проверка свободного места с учётом очереди. Готовый файл переносится в хранилище через `os.replace`, без копирования: скан 3 ГБ не занимает 6 ГБ. Файл, который не открывается через OpenSlide, удаляется и в каталог не попадает.
- Тесты: `tests/access_test.py` (69 проверок прав и групп), `tests/upload_test.py` (65 проверок загрузки, форматов и этикетки), `tests/smoke_test.py` (19). Первые два работают на временной базе и временном хранилище, рабочие данные не трогают. `tests/synthetic_svs.py` собирает настоящий минимальный SVS (тайловый TIFF с описанием Aperio), поэтому тесты не зависят от сканов заказчика.

**С сервера и с компьютера заказчика `github.com` не открывается** (таймаут), а `api.github.com` работает. Поэтому `git clone` на сервере не проходит: код переносится архивом `curl -L https://api.github.com/repos/sajberex-eng/Viewer-SVS/tarball/main`, распаковывается с `--strip-components=1`, `config.yaml` и `.env` при этом не трогаются. Push с компьютера заказчика идёт через раз: повторять.

**Caddy по IP-адресу.** Клиент не отправляет имя сервера (SNI), поэтому в `Caddyfile` обязателен глобальный `default_sni {$VIEWER_HOST}`: без него Caddy обрывает соединение и curl показывает пустой ответ. `VIEWER_TLS=internal` даёт самоподписанный сертификат с SAN на IP; Windows-curl такое соединение не открывает («SNI is not supported by OS»), браузер открывает после подтверждения исключения.

Обязательной смены выданного пароля нет (решение заказчика 2026-09-19): колонка `must_change_password` удалена в схеме версии 3. Пользователь меняет пароль сам в профиле; смена обрывает прочие его сессии.

Интерфейс приведён к новому API и проверен в браузере:

- `catalog.js` — дерево папок, путь, поиск, карточки сканов; у администратора создание, переименование, перемещение и удаление папок и сканов, окно «Доступ», свободное место;
- `upload.js` — загрузка перетаскиванием и кнопкой, по два файла сразу, части по 8 МБ с контрольной суммой, докачка после обрыва, отмена, индикатор скорости;
- `dialog.js` — окна на `<dialog>`: ввод, подтверждение необратимых действий, списки отметок, показ выданного пароля;
- `admin.js` и `web/pages/admin.html` (маршрут `/admin`) — вкладки «Пользователи», «Группы», «Журнал» с выгрузкой в CSV;
- панель этикетки во вьювере: изображение запрашивается только по нажатию, поворот на 90°, кнопка неактивна, если этикетки в файле нет.

Проверено вживую на временной базе: загрузка скана через браузер, появление карточки, выдача доступа группе, дерево, вьювер с тайлами.

Локальная база очищена от сканов по решению заказчика: всё загружается через веб-интерфейс. `storage.root` теперь `./data/slides`, папкой управляет только приложение.

## Состояние вьювера

Сделано и проверено локально в браузере:

- тайл-сервер, дисковый кэш тайлов, миниатюры (каталог живёт в базе, синхронизации с папкой больше нет);
- вход по паролю, роли, отказ 401 без авторизации, ограничение перебора паролей;
- вьювер: разделы 6 и 7 ТЗ целиком, включая поворот и пипетку белого;
- каталог с поиском (кнопки «Обновить список» больше нет: сканы попадают в каталог при загрузке; проверка хранилища по кнопке — КД-5, этап 7);
- `tests/smoke_test.py` проходит.

Не проверено: частота кадров при движении ползунков в обычном браузере, сравнение увеличений с ImageScope (критерий приёмки), файлы Docker (не запускались ни разу).

## Следующие шаги по порядку

Этапы 1–3 ТЗ сделаны 2026-09-19; открытый вопрос раздела 15 один — у кого, кроме разработчика, будет SSH-ключ от сервера.

1. Этап 1 (развёртывание) — сделано 2026-09-19, сервис работает по HTTPS на IP-адресе. Что стоит на сервере: Docker с лимитом журналов, `/opt/viewer-svs` (код), `config.yaml` и `.env` только на сервере, тома `viewer-data` и `caddy-data`, ежедневная копия базы в `/opt/backups` через таймер systemd `viewer-backup.timer` (хранится 7 дней). Осталось: домен и переход на `VIEWER_TLS=acme`, замер показателей раздела 11 на реальных сканах. Начат 2026-09-19: вход по отдельному SSH-ключу проекта (короткое имя `viewer-svs` в `~/.ssh/config` заказчика), сетевой экран ufw (22, 80, 443), fail2ban для SSH, файл подкачки 2 ГБ (увеличен до 4 ГБ 2026-09-22 по просьбе заказчика: расчёт клеточности при 1 мкм берёт до 1 ГБ). Подробное текущее состояние сервера и его адрес хранятся в приватной памяти Claude, а не здесь: репозиторий публичный. Из PowerShell 5.1 многострочные команды на сервер передавать через Bash-инструмент (`ssh viewer-svs 'bash -s' <<'EOF'`): PowerShell теряет кавычки. Дальше: Проверить `Dockerfile`, `docker-compose.yml`, `Caddyfile` (не запускались ни разу); SSH только по ключам; сетевой экран (ufw), fail2ban, unattended-upgrades, файл подкачки 2 ГБ, лимит журналов Docker; ежедневная копия `data/viewer.sqlite3`; лимит кэша 2 ГБ, `open_slides: 4`; замер показателей раздела 11 на трёх слайдах. Первым делом SSH-ключ: пароль root в чате не запрашивать, заказчик сам кладёт открытый ключ на сервер. 2. Этап 2 (хранилище, доступ, экраны) и этап 3 (двухэкранный режим) — сделаны 2026-09-19.

### С чего начинать следующую сессию

**Этап 11 — оценка клеточности костного мозга по MarrowQuant 2.0 (раздел 13.8 ТЗ), начат 2026-09-22.** Решения заказчика 2026-09-22: считаем на нашем сервере (перенос алгоритма на Python), валидирует у патологов Центра; StarDist не делаем; разрешение 1 или 2 мкм на точку (10× или 5×) — выбрать по валидации, выше 20× не нужно; окраска — список с сокращениями, у ИГХ только маркер свободным текстом; инструмент только у H&E; все форматы, включая KFB (для сверки с QuPath KFB конвертируются в SVS). Там же решено: значок — знак процента, верхний кружок пустой (жир), нижний закрашен (клетка): `<circle cx="7" cy="7" r="3"/><circle cx="17" cy="17" r="3" fill="currentColor" stroke="none"/><path d="M19 5L5 19"/>`; сокращения H&E, АЭ, Гомори, Конго, ИГХ; расчёт запускают патологи и администратор, результат видят все с доступом; оценку патолога на глаз программа не собирает. Допуск сверки с оригиналом заказчик определит по итогам сверки после реализации (предложено 3 п. п.). «КК» заменено на «Конго» по просьбе заказчика.

**Шаг 1 этапа 11 (окраска списком, ОК-1…ОК-3) сделан, проверен на стенде и выложен на рабочий сервер 2026-09-22 (коммит `4e93709`, копия кода `/opt/backups/code-before-4e93709.tgz`).** После выкладки: схема 6, 9 учётных записей, 11 сканов (5 ИГХ с маркерами Ki-67, CD3, ALK (D5F3), CD4, CD8; у 6 окраска не указана — выбирает заказчик), 3 папки, 6 аннотаций, 564 записи журнала. Что внутри: `server/stains.py`, схема базы версии 6 (колонка `slides.ihc_marker`; перенос: `HE` → H&E, любой другой текст → ИГХ с этим маркером), в имени файла распознаются только HE, H&E, AE, GOMORI, CONGO (PAS не ИГХ — не угадываем), `showIf` у полей `formDialog`, подписи `stainShort` и `stainFull` в `i18n.js`. Если в названии скана задано своё имя, маркер туда не подставляется: название по умолчанию строится из имени файла, только пока `title` пуст. Вход на стенд без ввода пароля в форму: cookie `viewer_session` подписывается ключом стенда (`itsdangerous.TimestampSigner(secret).sign(base64(json))`) и ставится через `document.cookie`.

**Шаг 2 этапа 11 (перенос алгоритма) сделан и выложен на рабочий сервер 2026-09-22 (коммит `aa221d6`, копия кода `/opt/backups/code-before-aa221d6.tgz`).** Схема базы не менялась (6); после выкладки 9 учётных записей, 6 сканов, 2 папки, 6 аннотаций, 583 записи журнала (сканы ИГХ и папку с ними заказчик удалил сам через интерфейс 2026-09-22 до выкладки — видно в журнале). Команда в рабочем контейнере: `docker compose exec -T viewer python -X utf8 -m server.manage cellularity <ID> < /dev/null` — на 840a40f2941d дала те же 51,63 % / 32,12 %, кэш Numba лёг в `data/cache/numba` (768 КБ). Следом выложен `a44d032` (копия `/opt/backups/code-before-a44d032.tgz`): инструкция сверки с QuPath и чтение GeoJSON-списка без FeatureCollection. Что внутри: `server/cellularity.py` (шаги MarrowQuant с правилами ImageJ, список правил — в начале файла), `server/ij_watershed.py` (Numba: MaximumFinder SEGMENTED и EDM ImageJ построчно), `server/cellularity_slide.py` (контуры GeoJSON из QuPath или по миниатюре, чтение области полосами, площади, формулы, итог по сумме площадей), команда `server.manage cellularity`, `tests/cellularity_test.py` (27 проверок; теперь наборов тестов четыре). В requirements — `numpy<2`, `opencv-python-headless`, `numba`; в Dockerfile `NUMBA_CACHE_DIR=/app/data/cache/numba`. Сверка с настоящим ImageJ — все маски до точки на 5 областях скана 840a40f2941d; итог, находки, время и память — в разделе 13.8 ТЗ, «Шаг 2». Команда через временный контейнер на сервере: при разрешении оригинала 42 с и 494 МБ на 4 фрагмента, при 1 мкм — 117 с и 1003 МБ.

**Сверка с QuPath начата 2026-09-22:** на скане `fedf4993343e` (3 фрагмента, контуры заказчика) расхождение по клеточности не больше 0,7 п. п. на фрагменте и 0,44 п. п. на стекле; таблица и разбор — в разделе 13.8 ТЗ. Ждём ещё 3–4 скана от заказчика. Присланные им файлы: `/opt/mqref/work/user2.geojson` на сервере. Разбор GeoJSON чинился дважды: список вместо FeatureCollection и контур без класса.

**Этап 11, что дальше (по порядку):**

1. ~~Сверка с QuPath у заказчика (КЛ-11)~~ — **отменена решением заказчика 2026-09-23**: «после реализации сравним по факту, лучшая проверка — результат». Инструкция [docs/qupath-comparison.md](docs/qupath-comparison.md) остаётся на случай возврата. Временное на сервере (`/opt/mqref` с кусками скана пациента, образы Docker) можно удалять — предложено заказчику 2026-09-23, ждёт его слова.
1б. **Инструкция пользователя по клеточности сделана 2026-09-23**: `docs/cellularity-manual/` — `manual.html` (источник HTML, печать в PDF через Chrome `--headless --print-to-pdf`), `cellularity-manual.pdf`, снимки `prod-*.png` с рабочего сервера (прислал заказчик; на сервере данных пациентов нет). Сохранившаяся история: черновик писался со стендовыми снимками и ждал снимков с рабочего сервера: заказчик просил брать их оттуда (данных пациентов на сервере нет) и сам сделает контуры и расчёт на одном скане, затем скажет, на каком. Для снимков нужен вход на рабочий сервер: временная учётная запись через `manage add-user --generate` (удалить после) и Selenium с Chrome без окна (`--ignore-certificate-errors`, сертификат самоподписанный); PDF — Chrome `--headless --print-to-pdf`. Selenium поставлен в локальный `.venv` (не в requirements). После выкладки `3912774` (удаление расчёта, «Рассчитать заново», маски наверху) на сервере 1 расчёт заказчика.
1а. **Шаг 3 (интерфейс) и шаг 4 (CSV) сделаны 2026-09-23** — см. раздел 13.8 ТЗ, «Шаг 3». Схема базы 7 (виды аннотаций `tissue`/`artifact`, таблица `cellularity_runs`), `server/cellularity_runs.py`, `web/static/js/cellularity.js`, маршруты `/api/slides/{id}/cellularity…` и `/api/cellularity/export.csv`. Проверено на стенде в браузере и сквозным тестом в `tests/access_test.py` (настоящий расчёт на синтетическом SVS через API). Ловушки: `conn.executescript` фиксирует транзакцию — упавшая миграция оставляла базу наполовину перенесённой, теперь миграции идут по одному запросу (`_run_statements`); индекс переезжает вместе с переименованной таблицей — удалять до `ALTER TABLE … RENAME`; со вторым слоем в мире OpenSeadragon пересчёт координат через `viewport.imageToViewportCoordinates` пишет ошибку в консоль на каждый кадр — все пересчёты идут через `SlideView.image` (TiledImage, до открытия — viewport); тест миграции строит «старую» базу из текущей схемы — при новых таблицах их надо удалять в тесте руками. Стенд: сценарий в папке сессии строил синтетический скан «как костный мозг» через `build_svs(image=…)` (новый параметр), администратор `stand`/`stand-1234` в группе «Патологи», `viewer-only`/`viewer-1234`, порт 8012; вход через форму. Скриншоты панели браузера временами приходят увеличенным углом страницы — состояние надёжнее читать через `javascript_tool`.
2. **КЛ-7 сделан и выложен на рабочий сервер 2026-09-23** (коммиты `798cc0f`, `3d2b56b`, `01a3f48`, `7571489`; копии кода `/opt/backups/code-before-<коммит>.tgz`, база `viewer-2026-09-23.sqlite3.gz`; после выкладки схема 6, 9 учётных записей, 6 сканов, 2 папки, 6 аннотаций, 598 записей журнала). На сервере скан 840a40f2941d: разрешение оригинала 38 с и 313 МБ (было 42 с и 494 МБ), 1 мкм 137 с и 449 МБ (было 117 с и 1003 МБ), результат при оригинале совпал до сотых. Память расчёта 30 → 12 байт на точку (замер, таблицы и находки на настоящем скане — раздел 13.8 ТЗ, «КЛ-7»). Журналы замеров на сервере: `/opt/backups/cell-*.log`. Новый `server/cellularity_job.py`: отдельный процесс с пониженным приоритетом, очередь по одному, наблюдатель за памятью (`rss_mb`) и `RLIMIT_DATA`, предел времени, ход (`Progress`, `stage_fraction`), отмена (`threading.Event`), отказ по оценке до запуска (`cellularity_slide.estimate_peak_mb`: 150 МБ + `cellularity.PEAK_BYTES_PER_PIXEL` × точек крупнейшего фрагмента). `manage cellularity` идёт через него; `--in-process` — прежний путь. Строки области — `SlideRows` (по требованию, одно чтение ≤ `READ_BUDGET_PX`); `analyse` принимает и массив, и источник строк. Показ хода и отмена в панели — в шаге 3. Ловушки: синтетический SVS из тестов без пирамиды — при любом разрешении читается уровень 0, для замеров памяти это только всплеск чтения, а фрагменты там вдвое уже настоящих (8000–9000 точек), поэтому память проверять и на сервере; `RLIMIT_DATA` — виртуальная память, не RSS (упало на втором фрагменте при RSS ниже предела); куча glibc в долгом процессе дробится — `mallopt` в `_tune_malloc` и `malloc_trim` после фрагмента; Bash-инструмент на Windows съедает обратные косые в heredoc — сценарии правки писать файлом и запускать; `tests/cellularity_test.py` теперь запускает дочерние процессы (spawn), защита `if __name__ == "__main__"` обязательна; SSH-сессия с `sleep` внутри рвётся сбросом — длинное запускать через `nohup`, а опрашивать короткими сессиями; поправку одного файла на сервере можно проверить без выкладки: `docker compose run --rm --no-deps -T -v /tmp/<файл>:/app/server/<файл>:ro viewer python -X utf8 -m server.manage …`.
3. Шаг 3 — интерфейс (КЛ-1…КЛ-9, КЛ-13, КЛ-14): контуры «ткань» и «артефакт», запуск в отдельном процессе, маски тайлами, панель результата. Шаг 4 — выгрузка CSV (КЛ-12).
4. После сверки с QuPath — удалить с сервера временное (опись ниже).

**Опись: что где лежит по этапу 11 и когда удалять.**

| Что | Где | Зачем | Когда удалять |
| --- | --- | --- | --- |
| эталон в репозитории | `tools/mq_reference/` (Java, Dockerfile, prepare/compare/steps, README) | заново сверить с ImageJ после любой правки алгоритма | не удалять |
| jar ImageJ 1.54f и Colour Deconvolution 3.0.2 | сервер, `/opt/mqref/ij.jar`, `cd.jar` | запуск эталона | после сверки с QuPath, по решению заказчика |
| копии кода и кэш Numba для сверки | сервер, `/opt/mqref/code`, `full`, `numba-cache` | запуск сверки и команды без выкладки | вместе с `/opt/mqref` |
| **куски изображения скана заказчика** (`rgb.png`) и маски | сервер, `/opt/mqref/work/f*` (274 МБ) | входы эталона | **после сверки обязательно**: это копия пациентских данных |
| сценарии и журналы сверки | сервер, `/opt/mqref/work/*.py`, `*.log` | разбор расхождений | вместе с `/opt/mqref` |
| образы Docker `mqref` и `eclipse-temurin:21-jdk` | сервер (~1,7 ГБ) | Java с виртуальным экраном для эталона | после сверки |
| кэш Numba рабочего сервиса | том данных, `data/cache/numba` | без него каждая оценка на ~15 с дольше | не удалять; пересоздаётся сам |
| NumPy, OpenCV, Numba в локальном `.venv` | компьютер заказчика | тесты `cellularity_test.py` | не удалять (в `requirements.txt`) |
| прототип `mq_bench.py`, стенд окраски | папка сессии 2026-09-22 | заменены переносом и тестами | не нужны; сервер от прототипа очищен (`/tmp/mqb` удалён) |

Команды удаления временного на сервере — `tools/mq_reference/README.md`, раздел «Что сейчас лежит на рабочем сервере». Перед удалением свериться с описью: что-то может понадобиться для разбора расхождений с QuPath.

Ловушки шага 2: `xvfb-run` в контейнере зависает — экран запускать вручную (`Xvfb :99 & DISPLAY=:99 java …`); SSH с компьютера заказчика рвётся сбросом соединения (не Wi-Fi: потерь пакетов нет, сброс — на стороне роутера или провайдера) — длинное на сервере запускать через `nohup … &` с выводом в файл; `cv2.distanceTransform` в последнем знаке зависит от размера картинки — поэтому EDM перенесена из ImageJ; `cv2.fillPoly` закрашивает точки на контуре — заливка многоугольников своя, как PolygonFiller; разделение по частям равно разделению целиком, только если передать частям максимум карты расстояний и непустые уровни всей картинки; `/usr/bin/time` в образе вьювера нет — память мерить через `resource.getrusage`. PyPI с компьютера заказчика рвёт соединения на больших файлах (pip доустанавливает с повторами, `--timeout 300 --retries 30`), с сервера ставится сразу. Скан для проверок, указанный заказчиком: `840a40f2941d` (H&E, жира больше нормы, 20×).

**Этап 10 (раздел 13.7 ТЗ) сделан, проверен на стенде и выложен на рабочий сервер 2026-09-21 (коммит `4f51d23`, копия кода `/opt/backups/code-before-4f51d23.tgz`).** Схема базы не менялась, осталась версии 5: после выкладки на сервере 8 учётных записей, 6 сканов, 2 папки, 8 аннотаций, 401 запись журнала. Этапы 4–9 выложены раньше. **Работ по плану раздела 13 больше не осталось**; следующие задачи ставит заказчик.

Следом выложен коммит `dbbf689` (копия кода `/opt/backups/code-before-dbbf689.tgz`): окно по клавише «?» стало «Справкой» с разделами, в нём появился раздел «Сравнение двух сканов» — связь, привязка по ориентирам, `Shift` и общий курсор (ИН-9). На телефоне этого раздела нет: разделённого экрана там не бывает (М-1). Последним выложен `096cc93` (копия `/opt/backups/code-before-096cc93.tgz`): у `.icon-btn .icon` появился свой размер — 16 точек, на телефоне 19; без него значки рулетки, привязки и поворота растягивались на всю кнопку. Заказчик проверил и принял.

Заказчик проверяет вживую настоящей мышью: ощущение от общего курсора и от поправки связи с `Shift`, поворот на произвольный угол и отражение на настоящих сканах, рулетку на ИГХ.

Что сделано в этапе 10 (подробности и критерии приёмки — раздел 13.7 ТЗ):

- ползунок зума убран (Т-1); на его место в столбец увеличений встали «Рулетка» и «Поворот», верхняя панель не изменилась (Т-6);
- окошко поворота: угол −180°…180° ползунком и числом, кнопки −90°, +90°, 0°, отметка «Отразить» (Т-2, Т-3). Поворот — на обе половины, отражение — только на активную, помечено словом «отражено» в заголовке половины и в строке состояния; угол и отражение уходят в ссылку;
- рулетка (Т-4) и меню по правой кнопке на препарате (Т-5) — во временном слое `web/static/js/overlay.js` (там же ориентиры привязки и перекрестие общего курсора);
- вся математика связи — в новом `web/static/js/link.js`: `b = R(угол)·F·a + сдвиг` в микрометрах, `F` зеркалит x при отражении срезов друг к другу. Отсюда же поворот второй половины (С-9), привязка по ориентирам (С-10) и общий курсор (С-8). Масштаб не подбирается;
- поправка связи с `Shift` (С-11) запоминает положение по цели плавного хода (`targetCenterMicrons`), а не по текущему кадру: иначе отпускание Shift посреди анимации запоминало середину пути.

**Ловушки, найденные при работе над этапом 10:**

- библиотека просмотра зеркалит только рисование, но не пересчёт координат. Точка скана ↔ точка экрана считается через `SlideView.pixelFromImage` / `imageFromPixel`; без этого аннотации, рулетка и курсор встают на противоположный край. Положение щелчка (`canvas-click`) библиотека возвращает уже незеркальным — там пересчёт прямой, `imageFromPixel` применять нельзя. А вот холст библиотеки нарисован зеркально, поэтому пипетке положение щелчка возвращается обратно (`canvasPoint`);
- `setActive()` вызывает `setTool(null)`, а тот раньше сбрасывал режим разметки ориентиров: щелчок во второй половине выключал привязку. Теперь `setTool` сохраняет режим `landmarks`, пока идёт привязка;
- в `fitBind` зеркалирование применялось дважды — в самой функции и внутри `withOffset`. Нашлось расчётом на Node, в браузере выглядело бы как «привязка не сходится»;
- кнопка «Связать» затирала свой значок надписью (`textContent`) — ошибка с этапа 7; теперь меняются `aria-label`, `title` и `data-label`;
- обработчик горячих клавиш падал, если целью события оказывался не элемент (`event.target.closest` у `document`).

**Проверка математики связи без браузера.** Сценарий лежал в папке сессии: он импортирует `link.js` как `.mjs` и сверяет, что у соответственных точек обеих половин совпадает смещение от центра экрана — на всех сочетаниях отражений и углов, плюс восстановление привязки по 2 и 3 парам. Node в системе есть (`node --version` → v24). Тем же приёмом (`node --check` на копии с расширением `.mjs`) удобно проверять синтаксис модулей.

**Стенд для проверки.** Сценарий стенда в папке сессии: свой `config.yaml` во временной папке, администратор `stand` в группе «Патологи», обычный пользователь `viewer-only`, синтетические сканы 20×, 40× и без размера пикселя (`build_svs(aperio=False)`), сервер на порту 8012. Наполнение и запуск — разными процессами: `TestClient` уже проходит жизненный цикл приложения, и `uvicorn` в том же процессе падает на «threads can only be started once». В `.claude/launch.json` временно добавлялась конфигурация `viewer-stand`; после проверки — `git checkout -- .claude/launch.json`.

**Чего нельзя проверить в панели браузера:** анимации не идут, пока панель скрыта, поэтому плавный ход (стрелки, кнопки увеличений) не доходит до цели — надо перемежать действия снимками экрана, они заставляют страницу рисовать кадр. По той же причине холст OpenSeadragon читается пустым (`getImageData` даёт прозрачные пиксели), а `requestAnimationFrame` не срабатывает вовсе — скрипт с ожиданием кадра просто висит до тайм-аута. Состояние половин удобно читать не из внутренностей страницы, а из ссылки на поле зрения: в ней есть центр, увеличение, угол и отражение обеих половин.

**Замечания заказчика 2026-09-21 после показа этапа 10** (все учтены):

- кнопки инструментов вылезали за край панели увеличений: `.zoom-tools` был рядом, а в панели всего 44 точки внутренней ширины. Теперь это сетка во всю ширину, кнопки идут столбиком;
- кнопка 2× убрана из `FIXED_MAGNIFICATIONS`: на микроскопе увеличения начинаются с 4×. Клавиши стали `1`–`4`, поправлены Н-1, Н-8, справка и README;
- белое свечение вокруг новой аннотации — это отметка выбранной, с этапа 9; заказчик принял её за пометку «последней добавленной». Теперь щелчок по препарату мимо пометок снимает выделение (`AnnotationLayer.#onClick` при выключенных инструментах, `onSelect(null)`);
- «не понял, как сделать, чтобы рулетка сохранилась»: `setTool(null)` стирал законченное измерение. Теперь оно остаётся на препарате до `Esc` или нового измерения; в базу по-прежнему не пишется;
- панель увеличений сворачивается кнопкой (`.zoom-panel.is-collapsed > :not(.zoom-toggle) { display: none }`), по умолчанию развёрнута, выбор не запоминается — как у мини-карты и панели стёкол;
- **заказчик спросил, не перенести ли панель целиком в строку состояния.** Посчитали на стенде: панель 52 × 322 точки — 4 % половины при сравнении, а выросшая на 10 точек строка состояния отняла бы 1280 × 10 по всей ширине; чистый выигрыш меньше четверти процента экрана. Решение заказчика 2026-09-21: **панель остаётся столбиком слева** (раздел 5.1 ТЗ, компоновка как в ImageScope). Если вопрос вернётся — цифры здесь.

Прежние решения заказчика по этапу 10 (2026-09-20), по которым всё и сделано:

- ползунок увеличения убрать, кнопки увеличений оставить (Т-1);
- поворот на произвольный угол (Т-2) и зеркальное отражение (Т-3) — одной кнопкой «Поворот» с окошком вместо двух кнопок ⟲⟳; `setFlip` в нашей версии OpenSeadragon есть; отражённый скан обязательно помечать;
- рулетка — временное расстояние между двумя точками, всем пользователям (Т-4); масштабная линейка внизу остаётся;
- меню по правой кнопке на препарате: «Рулетка», «Стрелка», «Контур» (Т-5);
- **верхнюю панель не загромождать** (Т-6): новое идёт в столбец увеличений и в меню по правой кнопке;
- автоматическое совмещение срезов и третий экран — не делаем;
- **общий курсор (С-8) делаем**: заказчик сначала отказался, затем в тот же день вернул. Перекрестие во второй половине там, куда указывает мышь в первой; считается тем же преобразованием, что и связь, поэтому делается последним, после привязки;
- **цель связи половин:** патолог идёт по ИГХ одного маркера и сравнивает с другим маркером на том же увеличении, участок к участку; до клетки срезы не сопоставить. Привязку делаем: С-10 (по ориентирам: две пары точек для неотражённых срезов, третья определяет отражение и показывает расхождение — так считает и патолог-тестер; расчёт в микрометрах, масштаб из размера пикселя, по точкам не подбирается) и С-11 (с зажатым `Shift` двигается и поворачивается только активная половина, связь запоминает новое положение). Хранения привязки на сервере не будет.

Вопросы этапа закрыты 2026-09-20: рулетка включается из меню по правой кнопке, клавишей `M` или кнопкой, точки — обычными щелчками, двойной щелчок по-прежнему приближает (`dblClickToZoom: true` не трогать); поворот в режиме сравнения синхронный, на обе половины, а взаимный угол задаётся привязкой (С-10, С-11). Порядок работы прежний: стенд, три набора тестов, сверка с критериями; коммит, пуш и деплой — после подтверждения заказчика.

Кнопка «Привязка по ориентирам» поставлена в столбец увеличений и видна только при сравнении двух сканов, у которых известен размер пикселя: в верхнюю панель её добавлять нельзя (Т-6), а меню по правой кнопке принадлежит одной половине, тогда как привязка — обеим.

**Панель браузера теперь берёт `launch.json` из папки проекта** (`Viewer SVS/.claude/launch.json`, он в git), а не из корня `GrowVibeProjects`; незнакомое имя конфигурации она молча заменяет первой в файле — так 2026-09-20 вместо стенда на несколько секунд поднялся обычный локальный сервер, и локальная база перешла на схему 5. Для стенда: временно дописать конфигурацию `viewer-stand` (запуск сценария из папки сессии, который задаёт `VIEWER_CONFIG` и стартует uvicorn на 8012), после проверки вернуть файл `git checkout -- .claude/launch.json`.

Решения заказчика 2026-09-20 после выкладки этапа 9 (сделано и выложено, коммит `2afc249`):

- указатель рисуется **стрелкой**: вершина стоит на отмеченной точке, древко уходит вверх-влево и клетку не закрывает. Фигура в `annotations.js` (`ARROW_HEAD`, `ARROW_SHAFT`);
- аннотации видят все, кому открыт доступ к скану (так и было сделано, правок не потребовалось);
- цвет аннотации (2026-09-20, схема базы версии 5, колонка `annotations.color`): по умолчанию неоново-зелёный `#39ff14` без обводки (решение заказчика: контраста хватает; проверено на почти белом фоне), в окне «Изменить» — ярко-красный; цвет задан переменной `--annot-color` у `.annot-layer`, красный — классом `is-red`. Выбранная аннотация цвет не меняет, вокруг неё белое свечение. Номер аннотации — её место в списке скана (порядок `created_at, rowid`), в базе не хранится: после удаления следующие номера сдвигаются. Поле `type: 'select'` в `formDialog`. Коммит `08fcc7f`, выложен на рабочий сервер 2026-09-20 (копия кода `/opt/backups/code-before-08fcc7f.tgz`), миграция 4 → 5 на рабочей базе прошла без потерь;
- список аннотаций сортируется по `rowid`: время создания пишется с точностью до секунды, и пометки одной секунды вставали в случайном порядке;
- **продолжение аннотаций (бывший раздел 13.7) и желательные доработки (бывший этап 10) из ТЗ убраны** и перенесены в раздел 14 «Вне рамок». Сделанного заказчик считает достаточным. Если он передумает, список сохранён там же. Порядок работы прежний: каждый пункт проверять в браузере на временном стенде, после этапа три набора тестов и сверка с критериями приёмки по одному; коммит, пуш и деплой — только после подтверждения заказчика, деплой одним выпуском после всего этапа.

Этап 9 сделан 2026-09-20, коммит `313e31d`, выложен на рабочий сервер в тот же день (копия кода `/opt/backups/code-before-313e31d.tgz`). Миграция схемы 3 → 4 на рабочей базе прошла без потерь: `user_version = 4`, таблица `annotations` создана, связи целы, 7 учётных записей, 6 сканов, 356 записей журнала на месте. В группе «Патологи» 6 человек — инструменты у них есть. Заказчик проверяет перетаскивание вершины мышью. Что сделано:

- Схема базы версии 4: таблица `annotations` (геометрия — JSON-список точек в пикселях скана, автор продублирован текстом, как в журнале). `server/annotations.py`: права (группа «Патологи» или администратор), проверка геометрии и комментария. Маршруты в `main.py`, права на скан проверяются так же, как для тайлов.
- `web/static/js/annotations.js` — слой одной половины: контуры лежат в группе SVG с матрицей преобразования (на кадр пересчитываются три точки, а не все вершины), маркеры и ручки вершин — в экранных координатах, поэтому не растут с увеличением. Рисование идёт через события OpenSeadragon `canvas-click` и `canvas-double-click`, так что перетаскивание и щипок продолжают работать во время разметки.
- Двойной щелчок приходит после двух обычных, поэтому лишняя вершина убирается в обработчике `canvas-double-click`.
- Панель аннотаций (`viewer.js`): показ, инструменты, список с переходом к аннотации, правка комментария, удаление, ссылка на аннотацию. Клавиши: A — показ, Enter — замкнуть контур, Esc — отменить начатый.
- На телефоне инструментов и кнопок правки нет (А-16), панель ложится поверх препарата: в ряд с изображением ей не хватает ширины.

**Чего нельзя проверить в панели браузера:** перетаскивание вершины настоящей мышью — изображение перерисовывается только в момент снимка экрана, и ручки успевают сместиться между замером и нажатием. Путь правки проверен синтетическими событиями указателя. Ещё одна ловушка: координаты в `computer` считаются в кадре панели (например, 800 × 538), а не в эмулируемом окне (1280 × 860); множитель — `800 / innerWidth`.

Этап 8 сделан 2026-09-20, коммит `a07b9ee`, выложен на рабочий сервер в тот же день (копия кода `/opt/backups/code-before-a07b9ee.tgz`). Заказчик проверяет на своём телефоне (М-6). Что сделано:

- Ряд увеличений внизу (М-2) включается медиазапросом `(max-width: 700px), (max-height: 520px)`: у телефона лёжа (812 × 375) мало высоты, а не ширины, и по одной ширине правило не срабатывало. Ползунок и жёлтое поле увеличения там скрыты — увеличение написано в строке состояния.
- Строка состояния на телефоне показывает только увеличение (М-4); сообщения об ошибках остаются видимыми через `.status-tiles:not(.is-error) { display: none }`.
- Список сканов случая (М-8): открытый — `position: absolute` поверх препарата, свёрнутый — обычная полоска 34 точки в потоке (иначе он накрывал ряд увеличений и линейку). После выбора скана на телефоне закрывается сам.
- `updateCompareButton()`: на телефоне кнопки «Сравнить» нет совсем, на узком окне компьютера она неактивна с подсказкой «не хватает ширины».
- `applyCompareLayout()` теперь вызывается и при одном скане, и в конце вызывает `layoutTopbar()`: набор видимых кнопок меняется, и раскладку панели надо пересчитывать, иначе кнопки налезают на «Выход».
- В шапке вьювера на телефоне остаётся название скана, путь скрыт (он длиннее и есть в каталоге).

Этап 7 сделан 2026-09-20, коммит `1e71390`, выложен на рабочий сервер в тот же день (копия кода `/opt/backups/code-before-1e71390.tgz`). Значки заказчик принял. Что сделано:

- `web/static/js/icons.js` — значки панели рисуются линиями (`currentColor`), вставляются по атрибуту `data-icon`. Подпись осталась только у «Сравнить»; короткое название кнопки задаётся `data-i18n-label` и идёт в `aria-label` и в меню «ещё» (CSS `content: attr(data-label)`).
- `viewer.js`, `layoutTopbar()` — кнопки, которым не хватило ширины, переезжают в меню «ещё» (В-4). Измерение по `scrollWidth > clientWidth`, поэтому у `.topbar-actions` больше нет `overflow: hidden` (иначе меню обрезалось) и её дети не сжимаются (`flex: none`).
- Окна `infoDialog` и `chooseDialog` в `dialog.js`: сведения о скане и горячие клавиши (ИН-16, ИН-9), выбор второго скана щелчком по строке (ИН-17).
- Клавиши: `Q` вместо `Tab` (ИН-15), `Page Down`/`Page Up` — соседний скан папки (ИН-10), `L`, `I`, `C`, `S`, `?`. При открытом окне клавиши не перехватываются.
- `replaceActive()` теперь работает и когда половина одна: щелчок по скану в списке случая и Page Down меняют содержимое без перезагрузки страницы.
- Путь до папки скана приходит с сервера (`/api/slides/{id}` → `path`) и показан в шапке и у каждой половины; щелчок открывает `/?folder=<id>` (ИН-3). Размер файла сервер теперь отдаёт только администратору (ИН-11).
- Каталог: путь на карточке в «Все папки» и в поиске (ИН-2), подпись «окраска · формат увеличение · добавлен дата», одна кнопка «⋯» вместо трёх значков по наведению (ИН-7), перемещение папки (КД-4), строка «Эти сканы видят только администраторы» (ИН-8), «Проверить хранилище» (КД-5), от какой папки наследуется доступ (КД-7).
- Маршрут `/favicon.ico` отдаёт логотип (ИН-14). Координаты курсора из строки состояния убраны (КД-6).

**Осторожно с дублями ключей в `i18n.js`:** ключ `access.inheritedFrom` уже существовал, и новый с тем же именем молча перекрывался старым значением. Перед добавлением строки — искать её ключ по файлу.

Этап 6 сделан 2026-09-20, коммит `69eb75b`, выложен на рабочий сервер в тот же день (копии: `/opt/backups/code-before-69eb75b.tgz` и `viewer-2026-09-20.sqlite3.gz`). Заказчик проверяет вживую: прервать загрузку закрытием браузера и продолжить одним нажатием. Что сделано:

- `web/static/js/filestore.js` — ручки файлов (FileSystemFileHandle) в IndexedDB, ключ = id загрузки. `canRemember` ложно там, где нет `showOpenFilePicker` (Firefox, Safari): тогда всё как раньше.
- `upload.js`: Task принимает `{file, handle}`, ручка запоминается в `sync()` и забывается при завершении и отмене; Wake Lock в `UploadQueue.keepAwake()` (после отказа браузера не переспрашиваем до следующей загрузки).
- `catalog.js`: `pickFiles()` (File System Access API либо поле выбора), `handlesFromDrop()` — ручки при перетаскивании надо забирать синхронно, до первого `await`, иначе список `items` пуст; `enqueue()` сопоставляет файл с прерванной загрузкой по имени и размеру; `sameFile()` сверяет хвост через `POST /api/uploads/{id}/verify`; `offerRestart()` — окно «Файл не совпадает».
- Сервер: `uploads.verify_tail()` (ЗГ-5), загрузку продолжает любой администратор (ЗГ-4, `_row` больше не проверяет владельца, `start()` ищет загрузку без учёта пользователя), журнал `upload.resume` с логином начавшего.

**Что нельзя проверить в панели браузера Claude Code** (проверяет заказчик вживую): окно выбора файла и перетаскивание настоящей мышью — браузер выдаёт ручку файла только по действию человека; Wake Lock — замок даётся только видимой вкладке.

**Событие `close` у `<dialog>` в панели не приходит вообще** (проверено на чистом `<dialog>` без кода проекта): окно закрывается, `open` становится `false`, но код после `await confirmDialog(...)` не выполняется, и кажется, что сломана логика. Обходится досылкой события: `d.querySelector('button').click(); d.dispatchEvent(new Event('close'))`. Это среда, а не ошибка вьювера.

Этап 5 сделан 2026-09-20, коммит `f9dddd0`, выложен на рабочий сервер в тот же день. Заказчик проверяет СК-2 сам (нужен ли прогрев промежуточных уровней). Что сделано:

- `server/warmup.py` (СК-1) — после приёма файла один фоновый поток готовит миниатюру и тайлы всех уровней, где тайлов не больше 100. Пауза 20 мс между тайлами, чтобы не занимать ядро целиком. Прогрев ставится в очередь в обработчике `complete_upload`, сбой прогрева на загрузку не влияет. Ранее загруженные сканы: `python -m server.manage warm` (повторный запуск дёшев — готовые тайлы пропускаются).
- `server/perms.py` + подключение к базе на поток в `db.py` (СК-3) — учётная запись и `AccessIndex` живут в памяти процесса. Сбрасывает их middleware `drop_permission_cache` после любого запроса, кроме GET/HEAD и `/api/uploads*` (части файла идут по нескольку раз в секунду и на права не влияют). Срок годности 3 с — на случай правки базы командой `server.manage` в другом процессе. Сервис работает одним процессом, иначе кэш пришлось бы делать общим.
- СК-4 `immutable` в заголовке тайлов, СК-5 отметка «использован» не чаще раза в час, СК-6 `imageLoaderLimit: 8` (проверено: у половины экрана `jobLimit` 8; у мини-карты свой экземпляр OpenSeadragon, ограничение на него не распространяется — тайлов там единицы).
- Журнал сервера: uvicorn настраивает только свои журналы, поэтому `log.info` сервиса никуда не попадал. Теперь `create_app` настраивает корневой журнал, если его никто не настроил. Без этого прогрев не видно в журнале.
- Прогрев на KFB работает: разовый `manage warm` на сервере прошёл по всем четырём сканам, включая KFB (в выводе появляется «Succeed to Initialize KfbLib»). На Windows KFB по-прежнему не читается.

**Выкладка 2026-09-20.** Копия базы `systemctl start viewer-backup.service`, копия кода в `/opt/backups/code-before-<коммит>.tgz`, затем архив с `api.github.com` и `docker compose up -d --build`. Разовый прогрев: `docker compose exec -T viewer python -X utf8 -m server.manage warm` — 4 скана, 8–24 новых тайла на скан за 3–6 с (обзорные тайлы открытых раньше сканов уже лежали в кэше). Кэш тайлов на сервере 166 МБ при лимите 2 ГБ.

**Связь с GitHub с этого компьютера.** `api.github.com` работает всегда, `github.com:443` отвечает через раз: curl открывает, а `git push` в это же время может падать на «Could not connect». Лечится повторами — 2026-09-20 понадобилось 6 попыток. Прокси в системе нет, дело не в настройках git.

**СК-2 закрыт без работ 2026-09-20:** заказчик проверил на рабочем сервере — задержек нет, отображение быстрее, особенно на KFB. Прогрев промежуточных уровней не делаем.

Репозиторий пока публичный; заказчик сделает его закрытым позже, когда проект будет доработан (решение 2026-09-19, в том числе из-за GPL-зависимости ASlide). До тех пор правила прежние: ни адреса сервера, ни сканов, ни имён файлов заказчика в репозитории.

План — ТЗ редакции 4, раздел 13 (`PRD.md`), составлен 2026-09-19 по итогам аудита; отчёт удалён 2026-09-20 после выполнения этапов 4–9, остался в истории git: `git show c7c1ec5:docs/audit-2026-09-19.md`. Обозначения находок: ИН — интерфейс, СК — скорость, ЗГ — загрузка, КД — код.

Порядок этапов (обоснование в разделе 13.0 ТЗ):

4. **Исправление ошибок** — сделано и выложено на рабочий сервер 2026-09-19 (коммит `59d8738`). Не проверен один критерий: вопрос Chrome при закрытии вкладки во время загрузки — в панели браузера Claude Code он не появляется, заказчик проверяет сам на рабочем сервере. Вне плана в тот же день (коммит `22a9cc8`, выложен): форматы других сканеров, KFB через ASlide, кнопка «Этикетка» в обычном режиме (с этапа 3 она была только в заголовке половины, который виден лишь при сравнении). Заказчик проверяет на рабочем сервере загрузку KFB и этикетку.
5. **Скорость** — сделано, выложено и принято 2026-09-20 (коммит `f9dddd0`); СК-2 закрыт без работ. Прогрев миниатюры и обзорных тайлов после загрузки, проверка прав 5,8 мс → 0,045 мс по медиане с немедленным сбросом при изменении доступа. Заказчик проверяет СК-2 на настоящих сканах.
6. **Загрузка без повторного выбора файла** — сделано и выложено на рабочий сервер 2026-09-20 (коммит `69eb75b`). File System Access API + IndexedDB в Chrome и Edge, опознание файла по имени и размеру, сверка контрольной суммой, Wake Lock, докачка чужой загрузки.
7. **Интерфейс** — сделано и выложено на рабочий сервер 2026-09-20 (коммит `1e71390`). Верхняя панель значками с меню «ещё», путь скана, справка по клавишам, сведения о скане, меню папки и её перемещение, строка о закрытой папке, проверка хранилища. В-3 закрыт: заказчик посмотрел значки на отдельном листе и принял без замечаний.
8. **Телефон** — сделано и выложено на рабочий сервер 2026-09-20 (коммит `a07b9ee`). Ряд увеличений внизу, короткая строка состояния, список сканов поверх препарата, панель значками из этапа 7. Осталась проверка на настоящих iPhone и Android (М-6) — её делает заказчик.
9. **Аннотации для обучения** — сделано и выложено на рабочий сервер 2026-09-20 (коммит `313e31d`). Указатель и контур с комментарием, список, ссылка на аннотацию, правка и удаление, показ по клавише A.
10. **Инструменты просмотра и связь сканов** — сделано и выложено на рабочий сервер 2026-09-21 (коммит `4f51d23`). Поворот на любой угол и отражение, рулетка, меню по правой кнопке, привязка по ориентирам, поправка связи с `Shift`, общий курсор.

После каждого этапа: три набора тестов, проверка в браузере, коммит, развёртывание. Версия статики считается при старте сервера — после правки скриптов и стилей локальный сервер перезапускать.

## Запуск на новом компьютере

Папки `.venv` и `data/cache` с собой не переносить: окружение привязано к путям, кэш пересоздаётся. `data/viewer.sqlite3` хранит пользователей и каталог; без него нужно создать пользователя заново.

```
winget install --id Python.Python.3.12 -e --source winget --scope user
python -m venv .venv
.venv\Scripts\python.exe -m pip install -r requirements-dev.txt
```

В `config.yaml` поправить `storage.root` на папку со сканами на этом компьютере. Пользователь создаётся командой `.venv\Scripts\python.exe -m server.manage add-user <логин> --role admin` (пароль запрашивается в терминале, заказчик вводит его сам). Сервер: `.venv\Scripts\python.exe -m uvicorn server.main:app --port 8000` или конфигурация `viewer` из `.claude/launch.json` для панели браузера.

Запуск без `--reload`: после правки файлов `server/` сервер нужно перезапустить. Файлы `web/` подхватываются обновлением страницы.

## Оформление Центра гематологии

Логотип: `web/static/img/logo.svg` (вектор, перерисован по логотипу заказчика; используется в шапке, на странице входа и как значок вкладки). Фирменные цвета заданы переменными в начале `app.css`: `--brand` `#3f4e9e` (синий кольца), `--brand-dark`, `--brand-tint`, `--brand-accent` `#e8672b` (оранжевый конец ростка). Кнопки, фокус, рамка выбранного стекла и полоса сверху шапки берут `--accent`, то есть `--brand`. Поле вокруг препарата и фон остаются нейтрально-серыми: ТЗ требует не искажать восприятие окраски. Жёлтое поле увеличения (`--zoom-current`) повторяет ImageScope и не менялось. Официальный брендбук Центра не предоставлялся, цвета сняты с присланного логотипа.

## Двухэкранный режим (раздел 8 ТЗ)

Вьювер разделён: `slide-view.js` это одна половина экрана (изображение, увеличения, мини-карта, линейка, этикетка), `viewer.js` управляет одной или двумя половинами, активной половиной, связью и ссылкой. Разметка половины лежит в `<template id="paneTemplate">`, элементы внутри ищутся по `data-role`, а не по `id`: половин бывает две.

- Панель настройки изображения одна на странице, `AdjustPanel.attachTo()` переключает её на активную половину. Значения берутся из `localStorage` по ключу половины.
- Связь (С-6, С-7): положение запоминается относительным в микрометрах в момент связывания, увеличение уравнивается в микроскопических единицах. Синхронизация идёт **без анимации** (`immediately = true`), иначе вторая половина догоняет первую рывками.
- `viewport.resize()` принимает РАЗМЕР, а не флаг: вызов `resize(true)` ломает пересчёт зума и даёт `NaN` в увеличении. Размер контейнера OpenSeadragon отслеживает сам (`autoResize`), поэтому после смены ширины достаточно `forceRedraw()` на следующем кадре.
- Панель увеличений одна на весь экран (`viewer.js`, `initZoomPanel`) и действует сразу на обе половины: сравнивать сканы имеет смысл только в одинаковом масштабе. Текущее увеличение каждой половины показано в её заголовке. Решение заказчика от 2026-09-19.
- В панели браузера Claude Code анимации не проигрываются (нет кадров в фоне), поэтому кнопки увеличений и связь там «не работают». Проверять надо прямыми вызовами с `immediately = true` либо на переднем плане вкладки.
- При проверке кнопок увеличения легко обмануться: значение ниже «вписать» (Fit) ограничивается до Fit, и кажется, что кнопка не сработала. У скана 2048 px в половине шириной 650 Fit это уже 16×, поэтому кнопки 2×, 4× и 10× ничего не меняют — это правильное поведение (Н-1), а не ошибка.
- Мини-карте нужны явные `navigatorWidth`/`navigatorHeight` и `navigatorAutoResize: false`: иначе OpenSeadragon растит её от ширины окна, и в режиме сравнения она закрывает соседнюю половину.
- Статика отдаётся с `Cache-Control: no-cache` (`FreshStatic` в `main.py`): иначе после обновления сервиса браузеры продолжают выполнять старые скрипты. ES-модули кэшируются отдельно от обычных запросов, `fetch(..., {cache:'reload'})` их не обновляет.

## Что важно знать о коде

- `server/tiles.py` заменяет `openslide.deepzoom.DeepZoomGenerator`. У Aperio шаг уровней 4,0001 вместо 4, и стандартный генератор читал полное разрешение: 190 мс на тайл вместо 15–30 мс. Не возвращать стандартный генератор.
- Коррекции изображения: OpenSeadragon работает с `drawer: 'canvas'`, а `web/static/js/adjust.js` после каждого кадра (событие `update-viewport`) копирует этот canvas в текстуру и рисует шейдером на WebGL-canvas поверх. Поэтому не переключать drawer на `webgl`. Порядок коррекций закреплён в ТЗ (И-5), формулы описаны в README; шейдер сверен с расчётом на JS, расхождение 0.
- Клавиатуру OpenSeadragon отключает обработчик `canvas-key`; все горячие клавиши в `initHotkeys` по `event.code`, чтобы работали в русской раскладке.
- Ключ слайда живёт только на сервере. В API уходят `id` (случайные 12 символов) и `title` из `slide_title()`. Исходное имя файла отдаётся только администратору. При добавлении полей в ответы API проверять, что имя файла туда не попало; это проверяет `access_test.py`.
- Окружение `.venv` привязано к путям: папка проекта была перенесена с другого компьютера, и окружение пришлось пересоздать (`python -m venv .venv` поверх). Если импорты вдруг перестанут работать, начинать с этого.
- Пароли: `bcrypt` напрямую. `passlib` несовместим с bcrypt 5, не добавлять.
- Строки интерфейса только через словарь `web/static/js/i18n.js` (ТЗ требует возможность казахского и английского).
- Панель браузера в Claude Code в фоне даёт около 2 кадров в секунду и пустой `event.code` у нажатий клавиш. Изображение дорисовывается с задержкой, горячие клавиши проверять через `dispatchEvent` с заполненным `code`. Это особенность среды, а не ошибка вьювера. Если OpenSeadragon показывает пустое поле (`lastDrawn: 0`, событий `tile-loaded` нет) при успешных запросах тайлов, вкладка просто не на переднем плане: `tabs_select` и повторный снимок всё показывают.
- Настройки изображения хранит сама половина экрана (`SlideView.adjustValues`, `setAdjust`); общая `AdjustPanel` только показывает и меняет значения активной половины (`attachTo(pane)`). Настройки из ссылки ставятся с `save: false`: у получателя они не записываются в браузер.
- Ссылка на два скана: сведения об обоих сканах запрашиваются до создания половин, иначе первая успевает открыться на весь экран и остаётся с вдвое большим увеличением. Половина при открытии сама сверяет размер области просмотра и вписывает изображение.
- Время в базе — UTC от `CURRENT_TIMESTAMP` без пояса. Наружу оно уходит через `db.utc_iso()` с отметкой `Z`, страница показывает его через `formatDateTime` из `i18n.js`. Новые поля со временем отдавать так же.
- Коды ошибок загрузки несут смысл для страницы (`UploadError(message, status)`): 409 — сверить принятое через `POST /api/uploads` и продолжать; 0, 5xx, 408, 429 — ждать связь (пауза 3 с → 1 мин, событие `online` будит сразу); 400, 401, 403, 404 — остановиться с ошибкой и кнопкой «Повторить». Запросы загрузки идут своей функцией `request()` в `upload.js`, а не через `api()`: истёкшая сессия не должна уводить со страницы посреди загрузки.
- Проверка в браузере без рабочих данных: временный стенд в папке сессии — свой `config.yaml` с `data_dir` и `storage.root` во временной папке, администратор и синтетические сканы (`tests/synthetic_svs.py`) заводятся через API, сервер запускается с `VIEWER_CONFIG`. Панель браузера берёт `launch.json` из папки проекта (до 2026-09-20 брала из корня `GrowVibeProjects`); как поднять стенд — в разделе «С чего начинать следующую сессию». Страница не может обращаться к другим портам (CSP `default-src 'self'`), поэтому тестовый файл для загрузки отдаётся самим стендом и удаляется после проверки. Список загрузок при ширине меньше 700 точек скрыт (М-5): для проверки загрузки расширять окно.
- При удалении файлов из `data/cache` инструмент PowerShell может отказать («protected path»). Кэш не мешает: пространство имён тайлов включает размер и дату файла и параметры нарезки.

## Проверка после изменений

```
.venv\Scripts\python.exe -X utf8 tests\access_test.py
.venv\Scripts\python.exe -X utf8 tests\upload_test.py
.venv\Scripts\python.exe -X utf8 tests\smoke_test.py
.venv\Scripts\python.exe -X utf8 tests\cellularity_test.py
```

Тест прав обязателен после любых правок `access.py`, `catalog.py` и маршрутов: он проверяет, что чужой скан не отдаётся ни сведениями, ни тайлом, ни миниатюрой, ни этикеткой.

Изменения интерфейса проверять в браузере на скане 40× и на скане 20× (у второго кнопка 40× должна быть неактивна, предел зума 40,0×).
